{
  "name": "genearchive-connector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "ui"
  ],
  "scripts": {
    "build": "npm run -w ui build && npm run -w server build",
    "dev:server": "npm run -w server dev",
    "dev": "npm run -w ui build && npm run -w server dev",
    "start": "npm run -w server start",
    "build:ui": "npm run -w ui build",
    "build:server": "npm run -w server build"
  }
}

